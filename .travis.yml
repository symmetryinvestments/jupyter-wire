language: d
sudo: false

matrix:
  include:
    - d: dmd-2.083.0
    - d: dmd-2.082.1
    - d: ldc-1.12.0

addons:
  apt:
    packages:
      - libzmq3-dev


script:
  - dub test --build=unittest-cov --compiler=${DC}

after_success:
 - bash <(curl -s https://codecov.io/bash)
